Virtual Machines ---

🔹 A virtual machine (VM) is software that simulates a physical computer.
🔹 Runs its own operating system (OS) and apps like a real computer.
🔹 Hosted on a physical machine (called the host).
🔹 Managed by a hypervisor (e.g., VirtualBox, VMware, KVM).
🔹 Each VM is called a guest machine.
🔹 Multiple VMs can run on a single physical machine.
🔹 Provides isolation – each VM runs separately and securely.
🔹Useful for testing, development, training, and server virtualization.
🔹Supports easy backup, cloning, and recovery.
🔹 Allows running different OSes (e.g., Linux on a Windows PC).


Server
Hypervisor and types--- type 1 ,type 2
